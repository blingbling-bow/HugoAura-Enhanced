实现希沃锁屏背景图自定义功能，共 3 个文件改动：

1. **新增 `src/aura/jsRewrite/network/appearance/customScreenLockBg.js`**
   - `type: "localResource"`，`urlPattern: "screenLock.js"`
   - `ruleFn(content, config)` 定位锚点 `t.handleListenScreenLockSource=function(e){var n=` 与 `;Object(S.a)(n,function(e){var n=P(e||{})` 之间的片段并替换为注入表达式
   - 注入逻辑：读 `window.__HUGO_AURA_CONFIG__.networkRewrite["appearance/customScreenLockBg"]`；`http(s)://` 开头的链接原样作为 CSS url()；本地路径做反斜杠转义后拼 `file:///`；未启用或未配置时保持原行为（集控下发图 → 内置默认图）
   - 反斜杠转义用 `String.fromCharCode(92)` 规避双层转义；锚点未命中时原样返回并打警告（版本容错）

2. **修改 `src/aura/init/shared/default.json`**
   - `networkRewrite` 下新增 `"appearance/customScreenLockBg": { "enabled": false, "backgroundPath": "" }`

3. **修改 `src/aura/ui/pages/configSubPages/disableLimitations/settings/auth.js`**
   - "屏幕锁"分类追加 index 7（switch「自定义锁屏背景」，restart: true）与 index 8（input「背景图片路径或链接」）
   - input 的 auraIf 依赖开关状态；callbackFn 校验：`http(s)://` 开头直接通过，本地路径用 `require("fs")` 校验存在且为 png/jpg/jpeg/bmp/webp/gif

验证：用真实解包的 `public/screenLock.js` 跑 ruleFn 断言注入结果语法有效；运行现有 28 个单测确认无回归。